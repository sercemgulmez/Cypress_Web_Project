# Cypress Web Project

Basic Cypress end-to-end testing project.

## Setup

```bash
npm install
```

## Commands

```bash
npm run cy:open
npm run cy:run
npm test
```

The sample test uses `https://example.cypress.io` as the default `baseUrl`.
